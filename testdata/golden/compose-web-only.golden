--- docker-compose.yml (mode 0644) ---
# generated by dockerizethis — edit freely
services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    networks:
      - default

networks:
  default:
    driver: bridge
