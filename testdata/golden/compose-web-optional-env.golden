--- docker-compose.yml (mode 0644) ---
# generated by dockerize — edit freely
services:
  app:
    build: .
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    ports:
      - "3000:3000"
    networks:
      - default

networks:
  default:
    driver: bridge
