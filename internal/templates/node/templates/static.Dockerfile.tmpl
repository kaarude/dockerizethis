{{template "stages" .}}
FROM nginx:alpine AS runtime
WORKDIR /app
ENV NODE_ENV=production
COPY nginx.conf /etc/nginx/nginx.conf
COPY --from=build --chown=1001:1001 /app/{{.StaticDir}} /app
USER 1001
EXPOSE {{.Port}}
ENTRYPOINT ["nginx"]
CMD ["-g", "daemon off;"]
